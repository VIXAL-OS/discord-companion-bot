# Keep the build context (and image) lean.
.git
.gitignore
.claude/
venv/
__pycache__/
**/__pycache__/
*.pyc
*.pyo

# Secrets + mutable state — bind-mounted via docker-compose.yml instead
.env
config.json
memories/
data/
logs/
*.log

# Docs / assets not needed at runtime
docs/
README.md
